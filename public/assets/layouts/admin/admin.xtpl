{{ extend('layouts/basic/basic') }}

{{ set (fullTitle = 'LetsBlog后台管理系统') }}

{{#block ('body')}}
	{{ css('./admin.css') }}
	{{ include('components/admin-header/admin-header') }}
	<div class="main">
		<h2 class="main__title">{{ block('mainTitle') }}</h2>
		<div class="main__body">{{ block('mainBody') }}</div>
	</div>
{{/block}}